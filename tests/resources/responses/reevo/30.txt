```python
import numpy as np

def heuristics_v2(distance_matrix: np.ndarray) -> np.ndarray:
    alpha = 0.5
    beta = 2

    heuristics_matrix = np.exp(-alpha * distance_matrix) * (1 / (1 + beta * distance_matrix))

    return heuristics_matrix
```