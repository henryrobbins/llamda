```python
import numpy as np

def heuristics_v2(distance_matrix: np.ndarray) -> np.ndarray:
    n = len(distance_matrix)
    heuristics_matrix = np.zeros((n, n))
    
    for i in range(n):
        for j in range(n):
            if i != j:
                heuristics_matrix[i][j] = 1 / (distance_matrix[i][j] + 1)
    
    return heuristics_matrix
```